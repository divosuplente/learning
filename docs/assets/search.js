
const SEARCH_INDEX = [{"slug": "00-java-foundations", "num": "00", "title": "Java Foundations", "content": "Module 00: Java Foundations ¶ What You'll Learn ¶ What programming is and why we use Java How to install the Java Development Kit (JDK) and an IDE Your first Java program — Hello World Variables and primitive types Reference types (String, arrays) Operators (arithmetic, relational, logical, ternary) Control flow: if/else, switch, loops Methods: parameters, return types, void Object-Oriented Programming: classes, objects, constructors The four OOP pillars: encapsulation, inheritance, polymorphism, abstraction Interfaces and abstract classes Java collections: List, Set, Map Java 21 records Enums Exception handling Generics basics The var keyword Packages and imports Prerequisites ¶ A computer running macOS, Linux, or Windows No prior programming experience required — we start from zero 1. What Is Programming? ¶ Programming is the act of writing instructions for a computer to execute. A computer is very fast at following instructions, but it needs you to write them in a language it understands. You cook a meal by following a recipe step by step. Programming is the same — you write a recipe (called a program ) and the computer (the chef) executes it. What Is Java? ¶ Java is a programming language created in 1995 by Sun Microsystems (now owned by Oracle). It is one of the most popular programming languages in the world, used by millions of developers to build: Backend services (the server side of web applications) Android mobile apps Enterprise software (banking, e-commerce, healthcare) Big data processing systems Why Java? ¶ Feature Benefit Write once, run anywhere Java compiles to bytecode that runs on any operating system with a JVM Strongly typed The compiler catches errors before the code runs Object-oriented Organizes code into reusable components (classes) Huge ecosystem Thousands of libraries and frameworks (like Spring Boot) Large community Easy to find help, tutorials, and answers The JVM, JDK, and JRE ¶ These three terms are confusing for beginners, so let's clarify: Term Full Name What It Is Do You Need It? JVM Java Virtual Machine Runs Java bytecode on your computer Yes — it runs your programs JRE Java Runtime Environment JVM + core libraries (enough to run Java programs) Only if you just want to run, not compile JDK Java Development Kit JRE + compiler ( javac ) + development tools Yes — this is what you install as a developer Think of it this way: - The JVM is the engine in a car - The JRE is the engine + the basic parts needed to drive - The JDK is the engine + tools to build and modify the car 2. Installing the JDK and an IDE ¶ Installing the JDK ¶ We will use JDK 21 (the latest Long-Term Support version as of 2025). macOS (using Homebrew): brew install openjdk@21 Linux (Ubuntu/Debian): sudo apt install openjdk-21-jdk Windows: Download the installer from Adoptium (Eclipse Temurin) and run it. Verifying the Installation ¶ Open a terminal (Command Prompt on Windows, Terminal on macOS/Linux) and type: java -version You should see something like: openjdk version \"21.0.x\" 2024-xx-xx Installing an IDE ¶ An IDE (Integrated Development Environment) is a program that helps you write code — like a word processor but for programming. It highlights syntax, suggests completions, and helps find errors. We recommend IntelliJ IDEA Community Edition (free): Go to https://www.jetbrains.com/idea/download/ Download the Community Edition (free, not the Ultimate edition) Install it following the instructions for your operating system 3. Your First Java Program — Hello World ¶ Every programmer's first program prints \"Hello, World!\" to the screen. Let's do it. Creating the File ¶ In IntelliJ IDEA: 1. Click New Project 2. Name it ordermgmt 3. Select Java as the language 4. Select 21 as the JDK 5. Click Create IntelliJ creates a project with this structure: ordermgmt/ ├── src/ │ └── Main.java └── ordermgmt.iml The Hello World Program ¶ public class Main { public static void main(String[] args) { System.out.println(\"Hello, World!\"); } } Line-by-Line Explanation ¶ public class Main — declares a class named Main . In Java, every file contains one public class, and the file name must match the class name (so this must be in Main.java ). A class is a container for code — we'll explain classes in detail later. public static void main(String[] args) — this is the main method , the entry point of every Java program. When you run a Java application, the JVM looks for this method and executes it. public — this method can be accessed from outside the class static — this method belongs to the class itself, not to an instance of the class (we'll explain this when we cover OOP) void — this method does not return a value main — the method name (the JVM looks for exactly this name) String[] args — an array of text arguments passed to the program from the command line System.out.println(\"Hello, World!\"); — prints the text Hello, World! to the console (the screen) and moves to the next line. System is a built-in Java class that provides system-level functionality out is a field on System that represents the standard output (the console) println is a method that prints text and adds a newline at the end \"Hello, World!\" is a string literal — text enclosed in double quotes The semicolon ; marks the end of the statement (like a period at the end of a sentence) Running the Program ¶ In IntelliJ: click the green play button next to main . Or right-click and select Run 'Main.main()' . In the terminal: javac Main.java # Compiles the .java file into a .class file java Main # Runs the compiled program Output: Hello, World! 4. Variables and Primitive Types ¶ A variable is a named container that stores a value. You give it a name, a type, and assign a value. Primitive Types ¶ Java has 8 primitive types — basic building blocks that store simple values: Type Description Size Example Values int Whole numbers (no decimals) 32 bits 0, 42, -7, 1000000 long Very large whole numbers 64 bits 9000000000000L double Decimal numbers 64 bits 3.14, -0.001, 99.99 float Smaller decimal numbers 32 bits 3.14f boolean True or false 1 bit true, false char A single character 16 bits 'A', '3', '$' byte Small whole number 8 bits -128 to 127 short Medium whole number 16 bits -32768 to 32767 Declaring Variables ¶ public class Variables { public static void main(String[] args) { // Declare and assign in one step int age = 30; double price = 19.99; boolean isActive = true; char grade = 'A'; // Declare first, assign later long population; population = 8000000000L; // L suffix means \"long\" // Print the values System.out.println(\"Age: \" + age); System.out.println(\"Price: \" + price); System.out.println(\"Active: \" + isActive); System.out.println(\"Grade: \" + grade); System.out.println(\"Population: \" + population); } } Important Notes About Money ¶ Never use double or float for money. Floating-point numbers can have rounding errors: // PROBLEM: floating point rounding errors double a = 0.1 + 0.2; System.out.println(a); // Prints 0.30000000000000004, NOT 0.3! // SOLUTION: use BigDecimal for money (we'll cover this later) For money, always use java.math.BigDecimal . We'll introduce it when we build the Order Management domain. 5. Reference Types: String and Arrays ¶ Strings ¶ A String is a sequence of characters (text). It is a reference type , not a primitive — meaning it's an object with methods: String name = \"Alice\"; String greeting = \"Hello, \" + name + \"!\"; // Concatenation with + System.out.println(greeting); // Hello, Alice! System.out.println(name.length()); // 5 (the length method) System.out.println(name.toUpperCase()); // ALICE System.out.println(name.charAt(0)); // A (character at position 0) Arrays ¶ An array is a fixed-size collection of values of the same type: // Declare and initialize an array int[] numbers = {10, 20, 30, 40, 50}; // Access elements by index (starts at 0) System.out.println(numbers[0]); // 10 System.out.println(numbers[2]); // 30 // Change an element numbers[0] = 100; System.out.println(numbers["}, {"slug": "01-build-tools-and-project-setup", "num": "01", "title": "Build Tools & Project Setup", "content": "Module 01: Build Tools &amp; Project Setup ¶ What You'll Learn ¶ What a build tool is and why every Java project needs one How to install Maven and understand the pom.xml file How to use Spring Initializr to generate a project The exact project structure Spring Boot expects How to write a complete pom.xml with all dependencies for the Order Management System How to configure your application with application.yml How to run the application and enable hot-reload during development How to use Spring profiles for different environments How to install the JDK using SDKMAN Prerequisites ¶ Module 00: Java Foundations — you understand basic Java syntax, classes, records, and packages No prior experience with build tools, Maven, or Spring Boot required 1. What Is a Build Tool? ¶ When you write Java code, you create .java files. But to run them, several things need to happen: Compile — convert .java files into .class files (bytecode the JVM can execute) Resolve dependencies — download external libraries (like Spring Boot) your code uses Package — bundle your compiled code and dependencies into a single runnable file Run — execute the packaged application Test — compile and run your test code You could do all this manually with terminal commands, but it would be tedious and error-prone. A build tool automates these steps. Analogy ¶ Think of a build tool as a kitchen assistant. You write the recipe (your code), and the assistant: - Goes to the store to buy ingredients (downloads dependencies) - Prepares them (compiles the code) - Packs them into containers (packages into a JAR) - Follows your recipe (runs the application) - Tastes the dish (runs tests) The two most popular build tools for Java are Maven and Gradle . This course uses Maven because it's simpler to start with and widely used in enterprise environments. 2. Installing the JDK with SDKMAN ¶ Before we can build anything, we need the JDK (Java Development Kit) installed. In Module 00, you installed it directly. Now let's do it the professional way using SDKMAN — a tool for managing Java versions. Installing SDKMAN ¶ macOS / Linux: curl -s \"https://get.sdkman.io\" | bash source \"$HOME/.sdkman/bin/sdkman-init.sh\" Windows: Use Scoop or Chocolatey instead, or install the JDK manually from Adoptium . Installing Java 21 with SDKMAN ¶ # List available Java versions sdk list java # Install OpenJDK 21 sdk install java 21.0.4-tem # Verify installation java -version # openjdk version \"21.0.4\" ... # Set it as the default sdk use java 21.0.4-tem SDKMAN lets you install multiple JDK versions and switch between them — useful when working on different projects that require different Java versions. 3. What Is Maven? ¶ Maven is a build automation tool for Java projects. It uses an XML file called pom.xml (Project Object Model) to describe: - Your project's name, group, and version - What external libraries (dependencies) your project needs - What plugins to use (for compiling, testing, packaging) - What Java version to target Installing Maven ¶ macOS (Homebrew): brew install maven Linux (Ubuntu/Debian): sudo apt install maven Windows: Download from https://maven.apache.org/download.cgi and add to PATH. Verifying Maven ¶ mvn -version # Apache Maven 3.9.x 4. Maven Coordinates ¶ Every Maven project is identified by three coordinates: Coordinate Description Example groupId Your organization or project group com.example artifactId The project name ordermgmt version The project version 0.0.1-SNAPSHOT Think of it like an address: - groupId = the street name (your organization) - artifactId = the house number (your specific project) - version = which renovation of the house (which release) Together, these three coordinates uniquely identify any library in the Maven ecosystem. When you declare a dependency, you specify these three coordinates, and Maven downloads the library automatically. 5. Spring Initializr ¶ Spring Initializr (pronounced \"init-ee-al-izer\") is a web tool that generates a Spring Boot project skeleton for you. Instead of creating all the files manually, you fill in a form and download a ready-to-use project. Using Spring Initializr ¶ Go to https://start.spring.io Fill in the form: Project: Maven Language: Java Spring Boot: 3.x.x (latest stable) Group: com.example Artifact: ordermgmt Name: ordermgmt Package name: com.example.ordermgmt Java: 21 Add dependencies (click \"Add Dependencies\"): Spring Web — for building REST APIs Spring Data JPA — for database access PostgreSQL Driver — for connecting to PostgreSQL Spring for GraphQL — for GraphQL API support Spring for Apache Kafka — for Kafka messaging Spring Boot DevTools — for hot reload during development Lombok — NOT needed, skip it (we use Java records instead) Click Generate Download and unzip the project What Spring Initializr Creates ¶ When you unzip the downloaded file, you get: ordermgmt/ ├── src/ │ ├── main/ │ │ ├── java/ │ │ │ └── com/ │ │ │ └── example/ │ │ │ └── ordermgmt/ │ │ │ └── OrdermgmtApplication.java │ │ └── resources/ │ │ ├── application.properties (we'll change this to .yml) │ │ └── static/ (for static web files) │ └── test/ │ └── java/ │ └── com/ │ └── example/ │ └── ordermgmt/ │ └── OrdermgmtApplicationTests.java ├── pom.xml ├── mvnw (Maven wrapper script — lets you run Maven without installing it) ├── mvnw.cmd (Maven wrapper for Windows) └── .gitignore The Main Application Class ¶ package com.example.ordermgmt; import org.springframework.boot.SpringApplication; import org.springframework.boot.autoconfigure.SpringBootApplication; @SpringBootApplication public class OrdermgmtApplication { public static void main(String[] args) { SpringApplication.run(OrdermgmtApplication.class, args); } } Let's rename it to something clearer: package com.example.ordermgmt; import org.springframework.boot.SpringApplication; import org.springframework.boot.autoconfigure.SpringBootApplication; @SpringBootApplication public class OrderManagementApplication { public static void main(String[] args) { SpringApplication.run(OrderManagementApplication.class, args); } } What Does @SpringBootApplication Do? ¶ This single annotation does three things (you'll learn about each in later modules): @Configuration — marks this class as a configuration class (Module 02) @EnableAutoConfiguration — tells Spring Boot to automatically configure beans based on the dependencies on the classpath (Module 03) @ComponentScan — tells Spring to scan the com.example.ordermgmt package and sub-packages for Spring components (Module 02) For now, just know that this annotation is the starting point of every Spring Boot application. 6. The pom.xml — Complete File ¶ The pom.xml (Project Object Model) is the heart of a Maven project. Here's the complete, correct pom.xml for our Order Management System: &lt;?xml version=\"1.0\" encoding=\"UTF-8\"?&gt; &lt;project xmlns=\"http://maven.apache.org/POM/4.0.0\" xmlns:xsi=\"http://www.w3.org/2001/XMLSchema-instance\" xsi:schemaLocation=\"http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd\"&gt; &lt;modelVersion&gt;4.0.0&lt;/modelVersion&gt; &lt;!-- Parent: Spring Boot provides sensible defaults for everything --&gt; &lt;parent&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-parent&lt;/artifactId&gt; &lt;version&gt;3.3.4&lt;/version&gt; &lt;relativePath/&gt; &lt;/parent&gt; &lt;!-- Our project coordinates --&gt; &lt;groupId&gt;com.example&lt;/groupId&gt; &lt;artifactId&gt;ordermgmt&lt;/artifactId&gt; &lt;version&gt;0.0.1-SNAPSHOT&lt;/version&gt; &lt;name&gt;Order Management System&lt;/name&gt; &lt;!-- Java version --&gt; &lt;properties&gt; &lt;java.version&gt;21&lt;/java.version&gt; &lt;/properties&gt; &lt;dependencies&gt; &lt;!-- Spring Web: for building REST APIs (controllers, endpoints) --&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt; &lt;/dependency&gt; &lt;!-- Spring Data JPA: for database ac"}, {"slug": "02-dependency-injection", "num": "02", "title": "Dependency Injection", "content": "Module 02: Dependency Injection ¶ What You'll Learn ¶ The problem that dependency injection solves: tight coupling with the new keyword What dependency injection (DI) is and how it works Inversion of Control (IoC) and the Hollywood Principle The Spring IoC container: beans and application context Types of injection: constructor, setter, and field injection Why constructor injection is preferred Spring stereotype annotations: @Component , @Service , @Repository , @Controller @Autowired : what it does and why constructor injection doesn't need it Bean scopes: singleton, prototype, request, session Manual bean creation with @Configuration and @Bean Resolving ambiguous dependencies with @Qualifier and @Primary Circular dependencies and how to avoid them Life-cycle hooks: @PostConstruct and @PreDestroy Wiring the Order Management domain using Spring DI Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, interfaces, constructors Module 01: Build Tools &amp; Project Setup — you have a working Spring Boot project with application.yml No prior experience with Spring or dependency injection required 1. The Problem: Tight Coupling with new ¶ When a class creates its own dependencies using the new keyword, it becomes tightly coupled — it can't function without those specific implementations. Example: The Bad Way ¶ package com.example.ordermgmt.service; import com.example.ordermgmt.domain.Customer; import com.example.ordermgmt.repository.CustomerRepository; import com.example.ordermgmt.repository.OrderRepository; import com.example.ordermgmt.repository.ProductRepository; import org.slf4j.Logger; import org.slf4j.LoggerFactory; import java.util.List; public class OrderService { private static final Logger log = LoggerFactory.getLogger(OrderService.class); private final CustomerRepository customerRepository; private final ProductRepository productRepository; private final OrderRepository orderRepository; // BAD: OrderService creates its own dependencies with `new` public OrderService() { this.customerRepository = new CustomerRepository(); // hard-coded! this.productRepository = new ProductRepository(); // hard-coded! this.orderRepository = new OrderRepository(); // hard-coded! } public Customer findCustomer(Long id) { return customerRepository.findById(id); } } Why Is This Bad? ¶ Tight coupling — OrderService is permanently bound to these specific repository implementations. You can't swap them without rewriting OrderService . Hard to test — In a unit test, you can't replace the real repositories with mocks (fake versions). You'd need a real database to test findCustomer() . Hidden dependencies — Someone who calls new OrderService() has no idea it needs three repositories. The dependencies are hidden inside the constructor. No flexibility — If you later need a different CustomerRepository (e.g., one that caches results), you'd have to change OrderService itself. The Fix: Dependency Injection ¶ Instead of OrderService creating its own dependencies, it receives them from outside: // GOOD: OrderService receives its dependencies through the constructor public class OrderService { private final CustomerRepository customerRepository; private final ProductRepository productRepository; private final OrderRepository orderRepository; // Dependencies are passed in — not created here public OrderService( CustomerRepository customerRepository, ProductRepository productRepository, OrderRepository orderRepository) { this.customerRepository = customerRepository; this.productRepository = productRepository; this.orderRepository = orderRepository; } } Now OrderService doesn't know or care how the repositories are created. It just declares \"I need these three things\" and someone else provides them. This is dependency injection . 2. What Is Dependency Injection? ¶ Dependency Injection (DI) is a design pattern where an object receives its dependencies from an external source instead of creating them itself. The Restaurant Analogy ¶ Think of a restaurant kitchen: Without DI: The kitchen grows its own tomatoes, raises its own chickens, and bakes its own bread. If the tomato crop fails, the kitchen can't make any tomato dishes. With DI: A supplier delivers tomatoes, chicken, and bread to the kitchen every morning. The kitchen doesn't care how the tomatoes were grown — it just uses them. If one supplier is unreliable, you switch to a different one without changing the kitchen. In software: - The kitchen is your class (e.g., OrderService ) - The ingredients are dependencies (e.g., CustomerRepository ) - The supplier is the Spring IoC container — it creates objects and delivers them to your classes 3. Inversion of Control (IoC) ¶ Inversion of Control (IoC) is the broader principle behind DI. It means that the control over creating and managing objects is inverted — moved from your code to a framework or container. The Hollywood Principle ¶ IoC is sometimes called the Hollywood Principle : \"Don't call us, we'll call you.\" In traditional programming, your code calls the framework. In IoC, the framework calls your code. You don't go looking for your dependencies — the container pushes them to you. Traditional (no IoC) With IoC OrderService calls new CustomerRepository() Spring creates CustomerRepository and gives it to OrderService Your code controls object creation The container controls object creation Your code knows about concrete implementations Your code only knows about interfaces/types 4. The Spring IoC Container ¶ The Spring IoC container is the \"supplier\" in our restaurant analogy. It: Discovers all components (classes annotated with @Component , @Service , etc.) Creates instances of them (beans) Figures out which beans depend on which other beans Injects the dependencies Key Terms ¶ Term Meaning Bean A Java object managed by the Spring container. Any object that Spring creates and can inject is a bean. Application Context The Spring container itself. It's the registry of all beans in your application. Wiring The process of connecting beans to their dependencies. Spring does this automatically. 5. Types of Injection ¶ There are three ways to inject dependencies into a class: 1. Constructor Injection (Preferred) ¶ Dependencies are passed through the constructor: @Service public class OrderService { private final CustomerRepository customerRepository; private final OrderRepository orderRepository; // Spring calls this constructor and passes the dependencies public OrderService( CustomerRepository customerRepository, OrderRepository orderRepository) { this.customerRepository = customerRepository; this.orderRepository = orderRepository; } } Pros: - Dependencies are final (immutable) — can't be changed after construction - You can't create an OrderService without its dependencies — the compiler enforces it - Easy to test — just pass mocks to the constructor in tests - Spring doesn't need @Autowired — if a class has one constructor, Spring uses it automatically 2. Setter Injection (Rarely Used) ¶ Dependencies are set via setter methods: @Service public class OrderService { private CustomerRepository customerRepository; @Autowired public void setCustomerRepository(CustomerRepository customerRepository) { this.customerRepository = customerRepository; } } Cons: The dependency can be changed at any time (not final ), and you can create an OrderService in an invalid state (without its dependencies). 3. Field Injection (Avoid) ¶ Dependencies are injected directly into fields: @Service public class OrderService { @Autowired // BAD — avoid this private CustomerRepository customerRepository; } Cons: The field can't be final (so it's mutable), you can't test without Spring (you'd need reflection to set the field), and it hides dependencies from the constructor signature. Why Constructor Injection Is Preferred ¶ Feature Constructor Setter Field Immutable ( final ) Yes No No Testable without Spring Yes Yes No Compile-time safety Yes No No Clear dependencies Yes No No Ru"}, {"slug": "03-spring-boot-fundamentals", "num": "03", "title": "Spring Boot Fundamentals", "content": "Module 03: Spring Boot Fundamentals ¶ What You'll Learn ¶ What Spring Boot is and how it differs from plain Spring Auto-configuration: how Spring Boot \"guesses\" what you need The @SpringBootApplication annotation in detail Creating REST endpoints: @RestController , @GetMapping , @PostMapping , etc. Request parameters: @RequestParam , @PathVariable , @RequestBody HTTP responses with ResponseEntity and status codes Input validation with Jakarta Bean Validation Exception handling with @RestControllerAdvice DTOs (Data Transfer Objects) as records Pagination and sorting Logging with SLF4J Spring Boot Actuator Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, exceptions Module 01: Build Tools &amp; Project Setup — you have a working Spring Boot project Module 02: Dependency Injection — you understand @Service , @Component , constructor injection 1. What Is Spring Boot? ¶ Spring Boot is a framework that makes building Java applications faster and easier. It is built on top of the Spring Framework , which has been the most popular Java framework since 2003. The Problem Spring Boot Solves ¶ Plain Spring requires a lot of manual setup: - Write XML configuration files - Manually configure every bean - Set up a web server (Tomcat, Jetty) separately - Manage dependency versions by hand - Configure database connections manually Spring Boot eliminates all this boilerplate. It uses convention over configuration — instead of telling Spring everything, you follow conventions and Spring Boot fills in the details automatically. Key Features ¶ Feature What It Means Auto-configuration Spring Boot configures beans automatically based on what's on the classpath Starter dependencies One dependency pulls in everything you need (e.g., spring-boot-starter-web ) Embedded server Tomcat is built in — no need to deploy a WAR file to an external server Production-ready Actuator provides health checks, metrics, and monitoring out of the box No XML Configuration is done in Java code or application.yml 2. Auto-Configuration ¶ Auto-configuration is Spring Boot's magic. When you add a dependency to your pom.xml , Spring Boot automatically creates and configures the necessary beans. How It Works ¶ Spring Boot scans your classpath (all the JAR files in your project) If it finds spring-boot-starter-web , it configures an embedded Tomcat server, Spring MVC, and JSON serialization If it finds spring-boot-starter-data-jpa , it configures a DataSource, EntityManager, and JPA repositories If it finds postgresql driver, it configures a PostgreSQL connection You don't write any configuration code for this — it just works. @SpringBootApplication ¶ package com.example.ordermgmt; import org.springframework.boot.SpringApplication; import org.springframework.boot.autoconfigure.SpringBootApplication; @SpringBootApplication public class OrderManagementApplication { public static void main(String[] args) { SpringApplication.run(OrderManagementApplication.class, args); } } @SpringBootApplication is a combination of three annotations: Annotation What It Does @Configuration Marks this class as a source of bean definitions @EnableAutoConfiguration Enables Spring Boot's auto-configuration @ComponentScan Tells Spring to scan the com.example.ordermgmt package and sub-packages for components ( @Controller , @Service , @Repository , etc.) When you run main() , Spring Boot: 1. Creates the Spring application context (the container that holds all beans) 2. Runs auto-configuration 3. Starts the embedded Tomcat server on port 8080 4. Your application is ready to receive requests 3. Creating REST Endpoints ¶ A REST endpoint is a URL that your application responds to. For example, GET /api/orders returns a list of orders. @RestController ¶ package com.example.ordermgmt.controller; import org.springframework.web.bind.annotation.*; @RestController @RequestMapping(\"/api/orders\") public class OrderController { // This class will handle all requests to /api/orders/* } @RestController — marks this class as a controller that returns data (JSON) instead of HTML pages. It's shorthand for @Controller + @ResponseBody . @RequestMapping(\"/api/orders\") — all URLs in this controller start with /api/orders . HTTP Method Mappings ¶ Annotation HTTP Method Example URL Purpose @GetMapping GET GET /api/orders Read data @PostMapping POST POST /api/orders Create new data @PutMapping PUT PUT /api/orders/1 Update existing data @DeleteMapping DELETE DELETE /api/orders/1 Delete data @PatchMapping PATCH PATCH /api/orders/1 Partially update data A Complete CRUD Controller ¶ package com.example.ordermgmt.controller; import com.example.ordermgmt.dto.CreateOrderRequest; import com.example.ordermgmt.dto.OrderResponse; import com.example.ordermgmt.service.OrderService; import jakarta.validation.Valid; import org.slf4j.Logger; import org.slf4j.LoggerFactory; import org.springframework.http.HttpStatus; import org.springframework.http.ResponseEntity; import org.springframework.web.bind.annotation.*; import java.util.List; @RestController @RequestMapping(\"/api/orders\") public class OrderController { private static final Logger log = LoggerFactory.getLogger(OrderController.class); private final OrderService orderService; // Constructor injection — Spring provides OrderService automatically public OrderController(OrderService orderService) { this.orderService = orderService; } // GET /api/orders — list all orders @GetMapping public ResponseEntity&lt;List&lt;OrderResponse&gt;&gt; listOrders() { log.info(\"list_orders request received\"); List&lt;OrderResponse&gt; orders = orderService.getAllOrders(); return ResponseEntity.ok(orders); } // GET /api/orders/{id} — get a specific order @GetMapping(\"/{id}\") public ResponseEntity&lt;OrderResponse&gt; getOrder(@PathVariable Long id) { log.info(\"get_order orderId={}\", id); OrderResponse order = orderService.getOrderById(id); return ResponseEntity.ok(order); } // POST /api/orders — create a new order @PostMapping public ResponseEntity&lt;OrderResponse&gt; createOrder(@Valid @RequestBody CreateOrderRequest request) { log.info(\"create_order request received\"); OrderResponse created = orderService.createOrder(request); return ResponseEntity.status(HttpStatus.CREATED).body(created); } // DELETE /api/orders/{id} — delete an order @DeleteMapping(\"/{id}\") public ResponseEntity&lt;Void&gt; deleteOrder(@PathVariable Long id) { log.info(\"delete_order orderId={}\", id); orderService.deleteOrder(id); return ResponseEntity.noContent().build(); } } 4. Request Parameters ¶ @PathVariable ¶ Extracts a value from the URL path: // GET /api/orders/42 @GetMapping(\"/{id}\") public ResponseEntity&lt;OrderResponse&gt; getOrder(@PathVariable Long id) { // id = 42 } @RequestParam ¶ Extracts a query parameter from the URL: // GET /api/orders?customerId=7&amp;status=PENDING @GetMapping public ResponseEntity&lt;List&lt;OrderResponse&gt;&gt; listOrders( @RequestParam(required = false) Long customerId, @RequestParam(required = false) String status) { // customerId = 7 (or null if not provided) // status = \"PENDING\" (or null if not provided) } @RequestBody ¶ Extracts the request body (JSON) and converts it to a Java object: // POST /api/orders with JSON body: {\"customerId\": 7, \"items\": [...]} @PostMapping public ResponseEntity&lt;OrderResponse&gt; createOrder(@Valid @RequestBody CreateOrderRequest request) { // request.customerId() = 7 // request.items() = list of items } @RequestHeader ¶ Extracts an HTTP header: @GetMapping public ResponseEntity&lt;List&lt;OrderResponse&gt;&gt; listOrders( @RequestHeader(value = \"Authorization\", required = false) String authHeader) { // authHeader = \"Bearer eyJhbGc...\" (or null) } 5. HTTP Responses with ResponseEntity ¶ ResponseEntity&lt;T&gt; lets you control both the response body and the HTTP status code: // 200 OK with a body return ResponseEntity.ok(orderResponse); // 201 Created with a body return ResponseEntity.status(HttpStatus.CREATED).body(orderResponse); // 204 No Content (n"}, {"slug": "04-repository-pattern", "num": "04", "title": "Repository Pattern", "content": "Module 04: Repository Pattern ¶ What You'll Learn ¶ What a database is and how relational databases work A crash course in SQL (just enough to understand what JPA does) What the Repository Pattern is and why it separates data access from business logic Object-Relational Mapping (ORM) and the impedance mismatch between objects and tables JPA and Hibernate: what they are and how they relate Spring Data JPA: what it gives you for free JPA entities: @Entity , @Id , @GeneratedValue , @Column , @Table , relationship annotations Creating entity classes for Customer, Product, Order, and OrderItem Spring Data repositories: JpaRepository interface and derived query methods Custom queries with @Query Pagination and sorting in repositories The N+1 problem and how to fix it Configuring PostgreSQL with Spring Boot Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, collections Module 01: Build Tools &amp; Project Setup — you have a Spring Boot project Module 02: Dependency Injection — you understand beans and constructor injection Module 03: Spring Boot Fundamentals — you understand application.yml and REST controllers 1. What Is a Database? ¶ A database is organized storage for your application's data. When your application shuts down, the data in memory (variables, objects) disappears. A database persists data — it saves it to disk so it survives restarts. Relational Databases ¶ The most common type of database is a relational database (RDBMS). It stores data in tables — think of a table as a spreadsheet: customers table: | id | name | email | address | |----|------|-------|---------| | 1 | Alice | &#97;&#108;&#105;&#99;&#101;&#64;&#101;&#120;&#97;&#109;&#112;&#108;&#101;&#46;&#99;&#111;&#109; | 123 Main St | | 2 | Bob | &#98;&#111;&#98;&#64;&#101;&#120;&#97;&#109;&#112;&#108;&#101;&#46;&#99;&#111;&#109; | 456 Oak Ave | products table: | id | name | price | stock | category | |----|------|-------|-------|----------| | 1 | Widget | 19.99 | 100 | Electronics | | 2 | Gadget | 49.99 | 50 | Electronics | | 3 | Book | 14.99 | 200 | Media | Key Concepts ¶ Concept Description Table A structured collection of rows and columns (like a spreadsheet) Row A single record (one customer, one product) Column A named attribute (name, email, price) Primary Key A column that uniquely identifies each row (usually id ) Foreign Key A column that references a primary key in another table, creating a relationship Relationships ¶ Tables relate to each other: A customer can have many orders (one-to-many) An order belongs to one customer (many-to-one) An order has many order items (one-to-many) An order item references one product (many-to-one) Customer 1 ────&lt; Order 1 ────&lt; OrderItem &gt;──── 1 Product &gt;──── 1 Product We use PostgreSQL — a powerful, open-source relational database. 2. SQL Crash Course ¶ SQL (Structured Query Language) is the language used to interact with relational databases. You don't need to be a SQL expert — that's what JPA does for you. But understanding the basics helps you understand what JPA does behind the scenes. SELECT — Read Data ¶ -- Get all customers SELECT * FROM customers; -- Get a specific customer SELECT * FROM customers WHERE id = 1; -- Get only name and email SELECT name, email FROM customers WHERE id = 1; INSERT — Create Data ¶ INSERT INTO customers (name, email, address) VALUES ('Charlie', 'charlie@example.com', '789 Pine St'); UPDATE — Modify Data ¶ UPDATE customers SET address = '999 New St' WHERE id = 1; DELETE — Remove Data ¶ DELETE FROM customers WHERE id = 1; JOIN — Combine Data from Multiple Tables ¶ -- Get orders with customer names SELECT o.id, o.status, c.name FROM orders o JOIN customers c ON o.customer_id = c.id; What JPA Does ¶ JPA generates SQL for you. Instead of writing SELECT * FROM customers WHERE id = 1 , you call customerRepository.findById(1L) and JPA generates the SQL automatically. 3. What Is the Repository Pattern? ¶ The Repository Pattern separates data access logic from business logic. Instead of writing SQL queries scattered throughout your application, you put all data access in one place — the repository . Without Repository Pattern ¶ // BAD — SQL everywhere public class OrderService { public Order createOrder(...) { Connection conn = DriverManager.getConnection(\"jdbc:postgresql://...\"); PreparedStatement stmt = conn.prepareStatement( \"INSERT INTO orders (customer_id, status) VALUES (?, ?)\"); stmt.setLong(1, customerId); stmt.setString(2, \"PENDING\"); stmt.executeUpdate(); // ... more SQL scattered everywhere } } With Repository Pattern ¶ // GOOD — clean data access public class OrderService { private final OrderRepository orderRepository; public OrderResponse createOrder(...) { OrderEntity order = new OrderEntity(); order.setCustomer(customer); order.setStatus(OrderStatus.PENDING); orderRepository.save(order); // JPA generates the INSERT return OrderResponse.from(order); } } The service doesn't know SQL — it just tells the repository \"save this\" and \"find that.\" The repository translates these calls into SQL. 4. ORM, JPA, and Hibernate ¶ ORM (Object-Relational Mapping) ¶ Java works with objects . Databases work with tables and rows . There's a mismatch: Java object: Order has a Customer field (a reference to another object) Database: orders table has a customer_id column (a number, a foreign key) ORM bridges this gap. It maps Java objects to database tables automatically: - order.getCustomer() → SQL JOIN to fetch the customer - orderRepository.save(order) → INSERT into orders table JPA (Java Persistence API) ¶ JPA is a specification (a set of interfaces) for ORM in Java. It defines annotations like @Entity , @Id , @OneToMany and the EntityManager interface. JPA itself doesn't do anything — it's just a contract. Hibernate ¶ Hibernate is the most popular implementation of JPA. It's the engine that actually does the work — it converts JPA annotations into SQL and executes it against the database. Spring Data JPA ¶ Spring Data JPA is a Spring module that makes JPA even easier. It provides the JpaRepository interface — you just define an interface and Spring Data generates the implementation automatically at runtime. Your Code → Spring Data JPA (interface) → JPA/Hibernate → SQL → PostgreSQL 5. JPA Entities ¶ A JPA entity is a Java class that maps to a database table. Each instance of the class represents a row in the table. Entity Anatomy ¶ @Entity // Marks this class as a JPA entity @Table(name = \"customers\") // Maps to the \"customers\" table public class CustomerEntity { @Id // This field is the primary key @GeneratedValue(strategy = GenerationType.IDENTITY) // Auto-increment @Column(name = \"id\") // Maps to the \"id\" column private Long id; @Column(name = \"name\", nullable = false, length = 100) private String name; @Column(name = \"email\", nullable = false, unique = true) private String email; @Column(name = \"address\") private String address; @Column(name = \"created_at\", updatable = false) private Instant createdAt; // Required by JPA — must have a no-argument constructor protected CustomerEntity() {} public CustomerEntity(String name, String email, String address) { this.name = name; this.email = email; this.address = address; this.createdAt = Instant.now(); } // Getters and setters public Long getId() { return id; } public void setId(Long id) { this.id = id; } public String getName() { return name; } public void setName(String name) { this.name = name; } public String getEmail() { return email; } public void setEmail(String email) { this.email = email; } public String getAddress() { return address; } public void setAddress(String address) { this.address = address; } public Instant getCreatedAt() { return createdAt; } public void setCreatedAt(Instant createdAt) { this.createdAt = createdAt; } } Key Annotations ¶ Annotation Purpose @Entity Marks the class as a JPA entity (maps to a table) @Table(name = \"x\") Specifies the table name (optional — defaults to class name) @Id Marks a field "}, {"slug": "05-service-oriented-architecture", "num": "05", "title": "Service-Oriented Architecture", "content": "Module 05: Service-Oriented Architecture ¶ What You'll Learn ¶ What an architecture pattern is and why software needs structure The difference between monolithic, microservices, and service-oriented architecture Layered architecture: Controller, Service, Repository, Database How to build a service layer with Spring Boot Transaction management with @Transactional Data Transfer Objects (DTOs) and why we use them Domain exceptions and custom exception hierarchies Application events for decoupled communication between services Common anti-patterns to avoid Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, interfaces, exceptions Module 01: Build Tools &amp; Project Setup — you have a working Spring Boot project Module 02: Dependency Injection — you understand @Service , @Component , constructor injection Module 03: Spring Boot Fundamentals — you can build REST controllers Module 04: Repository Pattern — you understand JPA entities and Spring Data repositories 1. What Is an Architecture Pattern? ¶ When you build a house, you don't just start placing bricks randomly. You follow a blueprint that says where the foundation goes, where the walls are, where the plumbing runs. The same is true for software. An architecture pattern is a blueprint for how to organize the code in your application. It defines: Which parts of the code talk to which other parts What responsibilities each part has How data flows through the system Without an architecture pattern, code becomes a tangled mess where everything depends on everything else. Changing one thing breaks five other things. This is called spaghetti code , and it is a nightmare to maintain. 2. Monolithic vs Microservices vs SOA ¶ Monolithic Architecture ¶ A monolith is a single application where all the code lives in one codebase and runs as one process. ┌─────────────────────────────────┐ │ Monolithic App │ │ ┌───────┐ ┌───────┐ ┌───────┐ │ │ │Orders │ │Users │ │Billing│ │ │ └───────┘ └───────┘ └───────┘ │ │ Single database │ └─────────────────────────────────┘ Pros: Simple to develop, deploy, and test. One codebase, one deployment. Cons: As the application grows, it becomes hard to understand and change. One bug can bring down the entire system. Microservices Architecture ¶ Microservices split the application into many small, independently deployable services, each with its own database. ┌──────────┐ ┌──────────┐ ┌──────────┐ │ Order │ │ User │ │ Billing │ │ Service │ │ Service │ │ Service │ │ (DB) │ │ (DB) │ │ (DB) │ └──────────┘ └──────────┘ └──────────┘ Pros: Each service is small and focused. Teams can work independently. Services can be deployed separately. Cons: Complexity. Distributed systems are hard. Network failures, data consistency, and debugging across services are all challenges. Service-Oriented Architecture (SOA) ¶ SOA is a middle ground. The application is a monolith (single deployment), but the code is organized into services — logical groupings of business functionality. Each service has its own layer (controller, service, repository), but they share a database and run in the same process. ┌─────────────────────────────────────────┐ │ Spring Boot App │ │ │ │ ┌─────────────────┐ ┌────────────────┐ │ │ │ Order Service │ │ Customer Service│ │ │ │ ┌────────────┐ │ │ ┌──────────┐ │ │ │ │ │Controller │ │ │ │Controller │ │ │ │ │ │ Service │ │ │ │ Service │ │ │ │ │ │ Repository │ │ │ │Repository │ │ │ │ │ └────────────┘ │ │ └──────────┘ │ │ │ └─────────────────┘ └────────────────┘ │ │ │ │ Shared Database │ └─────────────────────────────────────────┘ This is the approach we use in this course. It gives us clean separation of concerns without the complexity of distributed systems. 3. Layered Architecture ¶ The most common architecture pattern within a Spring Boot application is layered architecture . Each layer has a specific responsibility and can only talk to the layer directly below it. ┌──────────────────────────────────────────────┐ │ Client (Browser, Mobile) │ └──────────────────┬───────────────────────────┘ │ HTTP ┌──────────────────▼───────────────────────────┐ │ Controller Layer │ │ Receives HTTP requests, validates input, │ │ returns HTTP responses │ │ Does NOT contain business logic │ └──────────────────┬───────────────────────────┘ │ calls ┌──────────────────▼───────────────────────────┐ │ Service Layer │ │ Contains business logic, orchestrates │ │ repository calls, enforces rules │ │ This is the \"brain\" of the application │ └──────────────────┬───────────────────────────┘ │ calls ┌──────────────────▼───────────────────────────┐ │ Repository Layer │ │ Talks to the database, executes queries │ │ No business logic, just data access │ └──────────────────┬───────────────────────────┘ │ SQL ┌──────────────────▼───────────────────────────┐ │ Database │ │ PostgreSQL — stores your data │ └──────────────────────────────────────────────┘ The Golden Rule ¶ Each layer may only talk to the layer directly below it. Controllers call Services. Controllers do NOT call Repositories directly. Services call Repositories. Services do NOT return HTTP responses. Repositories talk to the Database. Repositories do NOT contain business logic. This rule keeps responsibilities clean and makes the code easy to test and maintain. Why Not Skip Layers? ¶ You might think: \"The controller just needs to list orders, why not call the repository directly?\" Because someday you'll need to add logic between the request and the database: - Check if the user is authorized to see these orders - Filter out cancelled orders - Cache the results - Send a notification when an order is viewed If the controller calls the repository directly, all that logic ends up in the controller. The controller becomes a 500-line mess. The service layer exists to keep the controller thin. 4. The Service Layer ¶ The service layer is where your business logic lives. In Spring Boot, a service is a class annotated with @Service that receives its dependencies through constructor injection. Let's build the OrderService for our Order Management System: package com.example.ordermgmt.service; import com.example.ordermgmt.domain.OrderStatus; import com.example.ordermgmt.domain.OrderEntity; import com.example.ordermgmt.domain.OrderItemEntity; import com.example.ordermgmt.domain.ProductEntity; import com.example.ordermgmt.domain.CustomerEntity; import com.example.ordermgmt.dto.CreateOrderItemRequest; import com.example.ordermgmt.dto.CreateOrderRequest; import com.example.ordermgmt.dto.OrderResponse; import com.example.ordermgmt.repository.OrderRepository; import com.example.ordermgmt.repository.ProductRepository; import com.example.ordermgmt.repository.CustomerRepository; import com.example.ordermgmt.service.exception.OrderNotFoundException; import com.example.ordermgmt.service.exception.InsufficientStockException; import com.example.ordermgmt.service.exception.CustomerNotFoundException; import com.example.ordermgmt.service.exception.ProductNotFoundException; import org.slf4j.Logger; import org.slf4j.LoggerFactory; import org.springframework.stereotype.Service; import org.springframework.transaction.annotation.Transactional; import java.math.BigDecimal; import java.time.Instant; import java.util.ArrayList; import java.util.List; @Service public class OrderService { private static final Logger log = LoggerFactory.getLogger(OrderService.class); private final OrderRepository orderRepository; private final ProductRepository productRepository; private final CustomerRepository customerRepository; // Constructor injection — Spring wires these automatically public OrderService( OrderRepository orderRepository, ProductRepository productRepository, CustomerRepository customerRepository) { this.orderRepository = orderRepository; this.productRepository = productRepository; this.customerRepository = customerRepository; } @Transactional public OrderResponse createOrder(CreateOrderRequest request) { log.info(\"create_order customerId={}\", request.c"}, {"slug": "06-kafka", "num": "06", "title": "Kafka", "content": "Module 06: Kafka ¶ What You'll Learn ¶ Synchronous vs asynchronous communication between system components What Apache Kafka is and the problems it solves Core Kafka concepts: topics, partitions, offsets, consumer groups, brokers How to set up Kafka locally with Docker How to configure Spring Boot to work with Kafka How to create Kafka producers that send messages How to create Kafka consumers that receive messages Serialization and deserialization of JSON messages Error handling with Dead Letter Queues Delivery semantics (at-least-once, at-most-once, exactly-once) How to make consumers idempotent How Kafka fits into the Order Management System Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, exceptions Module 01: Build Tools &amp; Project Setup — you have a working Spring Boot project Module 02: Dependency Injection — you understand Spring beans and constructor injection Module 03: Spring Boot Fundamentals — you understand application.yml configuration Module 05: Service-Oriented Architecture — you understand the service layer and application events 1. Synchronous vs Asynchronous Communication ¶ When two parts of a system need to communicate, there are two fundamental approaches: Synchronous Communication ¶ The caller sends a request and waits for a response before continuing. Like making a phone call — you wait on the line until the other person responds. Order Service ──\"create order\"──&gt; Payment Service &lt;──\"confirmed\"── (waiting...) Pros: Simple to understand. You get an immediate result. Cons: If the payment service is slow or down, the order service is stuck waiting. The services are tightly coupled — a failure in one cascades to the other. Asynchronous Communication ¶ The sender publishes a message and continues immediately without waiting for a response. Like sending an email — you send it and move on with your day. Order Service ──\"order created\"──&gt; [Message Broker] ──&gt; Payment Service │ └──&gt; Notification Service │ └──&gt; Analytics Service Pros: The sender doesn't wait. The receiver processes at its own pace. Multiple receivers can react to the same event. Services are loosely coupled . Cons: More complex. You need a message broker. eventual consistency (things happen eventually, not immediately). Harder to debug. When to Use Which? ¶ Scenario Use Need an immediate answer (e.g., \"does this customer exist?\") Synchronous Need to notify multiple systems something happened Asynchronous The receiver might be slow or temporarily down Asynchronous The caller needs the result to proceed Synchronous Triggering background work (emails, analytics, shipping) Asynchronous Apache Kafka is the most popular tool for asynchronous, event-driven communication in modern backend systems. 2. What Is Apache Kafka? ¶ Apache Kafka is a distributed event streaming platform. Think of it as a post office for your applications: Applications (producers) drop off messages (letters) at the post office (Kafka) The post office stores and organizes messages by category (topics) Other applications (consumers) pick up messages they're interested in Kafka was originally developed at LinkedIn to handle massive amounts of event data. It is designed to be: Fast — can handle millions of messages per second Durable — messages are stored on disk, surviving crashes Scalable — can run as a cluster of servers Reliable — messages can be replicated across multiple servers Kafka Analogy: A Newsstand ¶ Imagine a newsstand: Publishers (producers) deliver newspapers to the newsstand The newsstand organizes newspapers into sections: Sports, Politics, Tech (topics) Each section has a shelf with spots labeled 1, 2, 3, ... (partitions and offsets) Subscribers (consumers) come to the newsstand and read the sections they're interested in Each subscriber remembers where they stopped reading (offset) so they don't miss anything 3. Core Kafka Concepts ¶ Topic ¶ A topic is a category or feed to which messages are published. Topics are like folders for your messages. Topic: order-events ├── Partition 0: [msg1, msg2, msg3, msg4, msg5] ├── Partition 1: [msg1, msg2, msg3] └── Partition 2: [msg1, msg2, msg3, msg4, msg5, msg6, msg7] Partition ¶ Each topic is split into one or more partitions . A partition is an ordered, append-only log of messages. Partitions allow Kafka to scale — multiple partitions can be distributed across multiple servers. Topic: order-events (3 partitions) Partition 0: [OrderCreated] [OrderConfirmed] [OrderShipped] Partition 1: [OrderCreated] [OrderCancelled] Partition 2: [OrderCreated] [OrderConfirmed] [OrderShipped] [OrderDelivered] Messages within a single partition are ordered — you read them in the order they were written. But across partitions, there is no guaranteed order. Offset ¶ An offset is a sequential number that identifies each message within a partition. It's like a page number in a book. Partition 0: [msg at offset 0] [msg at offset 1] [msg at offset 2] [msg at offset 3] Consumers track their offset — they remember \"I've read up to offset 5\" so they know where to resume. Consumer Group ¶ A consumer group is a set of consumers that work together to read messages from a topic. Each partition is assigned to exactly one consumer within a group. Topic: order-events (3 partitions) Consumer Group \"notification-service\": Consumer 1 → reads Partition 0 Consumer 2 → reads Partition 1 Consumer 3 → reads Partition 2 Consumer Group \"analytics-service\": Consumer 1 → reads Partition 0, 1, 2 (single consumer reads all) This is how Kafka scales: add more consumers to a group, and they share the load automatically. Broker ¶ A broker is a Kafka server. A Kafka cluster consists of one or more brokers. For development, one broker is enough. In production, you typically run 3 or more brokers for reliability. Kafka Cluster: ┌──────────┐ ┌──────────┐ ┌──────────┐ │ Broker 1 │ │ Broker 2 │ │ Broker 3 │ │(leader) │ │(follower)│ │(follower)│ └──────────┘ └──────────┘ └──────────┘ Producer ¶ A producer is an application that sends messages to Kafka topics. In our system, the OrderService will be a producer — when an order is created, it sends an OrderCreatedEvent message to Kafka. Consumer ¶ A consumer is an application that reads messages from Kafka topics. In our system, we'll create an OrderEventConsumer that reads order events and simulates sending confirmation emails. Architecture Diagram ¶ ┌──────────────┐ ┌───────────────────┐ │ Order │ produces │ Kafka Cluster │ │ Service │───────────────────&gt;│ │ │ (Producer) │ OrderCreated │ Topic: │ └──────────────┘ Event │ order-events │ │ │ ┌──────────────┐ │ ┌───────────────┐ │ │ Notification│ consumes │ │ Partition 0 │ │ │ Service │&lt;───────────────────│ │ Partition 1 │ │ │ (Consumer) │ │ │ Partition 2 │ │ └──────────────┘ │ └───────────────┘ │ │ │ ┌──────────────┐ │ │ │ Analytics │ consumes │ │ │ Service │&lt;───────────────────│ │ │ (Consumer) │ │ │ └──────────────┘ └───────────────────┘ (Different consumer groups) 4. Setting Up Kafka Locally with Docker ¶ To run Kafka on your machine, we use Docker Compose — a tool that defines and runs multi-container Docker applications. You learned about Docker in Module 01. Modern Kafka (KRaft mode, no Zookeeper) can run with a simple docker-compose.yml : # docker-compose.yml services: kafka: image: apache/kafka:3.7.1 container_name: kafka ports: - \"9092:9092\" environment: KAFKA_NODE_ID: 1 KAFKA_PROCESS_ROLES: broker,controller KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093 KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092 KAFKA_CONTROLLER_LISTENERS: PLAINTEXT://0.0.0.0:9093 KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093 KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1 KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1 KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1 To start Kafka: docker-compose up -d To stop Kafka: docker-com"}, {"slug": "07-graphql", "num": "07", "title": "GraphQL", "content": "Module 07: GraphQL ¶ What You'll Learn ¶ What an API is and a recap of REST concepts from Module 03 The problems REST has: over-fetching and under-fetching What GraphQL is and how it solves REST's problems GraphQL vs REST: a side-by-side comparison Core GraphQL concepts: schema, types, queries, mutations, subscriptions The Schema Definition Language (SDL) How to model the Order Management domain with GraphQL Writing queries and mutations for orders, customers, and products Implementing resolvers in Spring Boot with @QueryMapping , @MutationMapping , and @SchemaMapping The N+1 problem in GraphQL and how to solve it with DataLoader Error handling in GraphQL GraphQL subscriptions with WebSockets Testing GraphQL endpoints with GraphiQL Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, interfaces Module 01: Build Tools &amp; Project Setup — you have a Spring Boot project Module 02: Dependency Injection — you understand Spring beans and constructor injection Module 03: Spring Boot Fundamentals — you understand REST controllers and DTOs Module 04: Repository Pattern — you understand JPA entities and repositories Module 05: Service-Oriented Architecture — you understand the service layer 1. What Is an API? (REST Recap) ¶ An API (Application Programming Interface) is a contract that defines how software components communicate. In Module 03, you learned about REST APIs , where: Each resource has a URL: /api/orders , /api/customers , /api/products You use HTTP methods to act on them: GET to read, POST to create, PUT to update, DELETE to remove Data is exchanged as JSON For example, to get order #42 in REST: GET /api/orders/42 Response: { \"id\": 42, \"customerId\": 7, \"customerName\": \"Alice\", \"status\": \"CONFIRMED\", \"totalAmount\": 129.99, \"createdAt\": \"2025-01-15T10:30:00Z\", \"items\": [ { \"id\": 1, \"productId\": 100, \"productName\": \"Widget\", \"quantity\": 2, \"unitPrice\": 49.99 } ] } REST works well and is widely used. But it has limitations. 2. Problems with REST ¶ Over-Fetching ¶ Over-fetching happens when the API returns more data than the client needs. Imagine a mobile app that shows a list of orders. It only needs the order ID and status — but the REST endpoint returns the entire order object including all items, the customer details, timestamps, and more. That's wasted bandwidth and slower response times. Client needs: { \"id\": 42, \"status\": \"CONFIRMED\" } REST returns: { \"id\": 42, \"customerId\": 7, \"customerName\": \"Alice\", \"status\": \"CONFIRMED\", \"totalAmount\": 129.99, \"createdAt\": \"...\", \"items\": [...] } ↑ wasted data Under-Fetching ¶ Under-fetching happens when the client needs to make multiple API calls to gather all the data it needs. Imagine a dashboard that shows orders with customer names and product images. With REST, you might need: GET /api/orders — get the orders (returns customer IDs, not names) GET /api/customers/7 — get customer details for order 1 GET /api/customers/12 — get customer details for order 2 GET /api/products/100 — get product details for the first item That's 4+ network round trips. Each one adds latency. The Root Cause ¶ The problem is that REST endpoints return a fixed shape . The server decides what data to return, regardless of what the client actually needs. Different clients (web app, mobile app, analytics dashboard) have different needs, but they all get the same response. 3. What Is GraphQL? ¶ GraphQL is a query language for your API. Instead of the server deciding what data to return, the client specifies exactly what it wants, and the server returns exactly that — nothing more, nothing less. Think of it like a restaurant: REST is like a set menu: you get whatever the chef decided to put on the plate. You might not want the salad, but it's there. GraphQL is like ordering a la carte: you pick exactly the dishes you want. Want just the steak? You get just the steak. A GraphQL Query Example ¶ Instead of GET /api/orders/42 , the client sends a query to a single GraphQL endpoint: POST /graphql { \"query\": \"{ order(id: 42) { id status } }\" } The server responds with exactly those fields: { \"data\": { \"order\": { \"id\": 42, \"status\": \"CONFIRMED\" } } } No over-fetching. The client asked for id and status , and that's all it got. A More Complex Query ¶ The client can also ask for related data in a single request — no under-fetching: { order(id: 42) { id status customer { id name } items { quantity unitPrice product { id name } } } } This single query replaces what would have taken 3+ REST calls. The server resolves all the relationships and returns the complete data in one response. 4. GraphQL vs REST: Comparison ¶ Dimension REST GraphQL Endpoints Many ( /api/orders , /api/customers , etc.) One ( /graphql ) Data shape Fixed by the server Chosen by the client Over-fetching Common Eliminated — client asks for only what it needs Under-fetching Common — multiple calls needed for related data Eliminated — related data in one query Versioning URL versioning ( /v1/ , /v2/ ) or headers Schema evolution — add fields without breaking old clients HTTP methods GET , POST , PUT , DELETE for different operations POST for everything (queries and mutations) Caching HTTP caching (browser, CDN) Needs custom caching (Apollo Client, etc.) Learning curve Lower — maps to HTTP concepts Higher — new query language, schema concepts Best for Simple CRUD APIs, resource-oriented services Complex data relationships, multiple client types File uploads Simple with multipart/form-data Requires custom handling When to Use GraphQL? ¶ You have multiple clients (web, iOS, Android) with different data needs Your data has complex relationships that require multiple REST calls to assemble You want to avoid versioning your API You want a single endpoint for the frontend to interact with When to Stay with REST? ¶ Your API is consumed by other servers (machine-to-machine) where over-fetching doesn't matter You need HTTP-level caching Your API is simple CRUD with few relationships Your team is not familiar with GraphQL You can also use both — Spring Boot supports having REST and GraphQL endpoints in the same application. 5. Core GraphQL Concepts ¶ Schema ¶ A schema is the contract of your GraphQL API. It defines what types exist, what queries are available, and what mutations you can perform. The schema is written in the Schema Definition Language (SDL) and lives in a .graphqls file. Types ¶ A type defines the shape of an object in your API. It's like a Java class definition: type Order { id: ID! status: OrderStatus! totalAmount: BigDecimal! customer: Customer! items: [OrderItem!]! } ID , String , Int , Float , Boolean are built-in scalar types ID! — the ! means this field is non-nullable (required) [OrderItem!]! — a non-nullable list of non-nullable items BigDecimal — we can register custom scalars for types Java has but GraphQL doesn't (like BigDecimal , Instant ) Customer is a custom type we define ourselves Queries ¶ A query reads data. It's the GraphQL equivalent of a GET request: type Query { order(id: ID!): Order orders(customerId: ID): [Order!]! customer(id: ID!): Customer products(category: String): [Product!]! } Mutations ¶ A mutation modifies data. It's the GraphQL equivalent of POST , PUT , or DELETE : type Mutation { createOrder(input: CreateOrderInput!): Order! confirmOrder(id: ID!): Order! cancelOrder(id: ID!): Order! createProduct(input: CreateProductInput!): Product! } Subscriptions ¶ A subscription is a real-time stream of data pushed from the server to the client over WebSockets. It's used for live updates like order status changes: type Subscription { orderStatusChanged(orderId: ID): OrderStatusChangedEvent! } We'll cover subscriptions briefly at the end of this module. 6. Schema Definition Language (SDL) ¶ Let's write the complete GraphQL schema for our Order Management System. Create a file at src/main/resources/graphql/schema.graphqls : # Custom scalars for types Java has but GraphQL doesn't scalar BigDecimal scalar Instant # ---"}, {"slug": "08-reactor-pattern", "num": "08", "title": "Reactor Pattern", "content": "Module 08: Reactor Pattern ¶ What You'll Learn ¶ What reactive programming is and how it differs from imperative (blocking) code Why the industry is moving toward reactive solutions (the C10K problem) Project Reactor — the library that powers Spring WebFlux Core reactive types: Mono (0-or-1 item) and Flux (0-to-N items) Creating and combining reactive streams with static factories and operators The reactive stream contract : onNext , onError , onComplete Backpressure — what it is and how Reactor handles it Threading and schedulers ( publishOn , subscribeOn , boundedElastic ) Error handling strategies ( onErrorReturn , onErrorResume , retry , retryWhen ) Hot vs. Cold publishers and when to use each Building reactive REST endpoints with Spring WebFlux Connecting reactive streams to Kafka and R2DBC When not to use reactive programming Debugging reactive pipelines ( log , checkpoint , doOnNext ) Prerequisites ¶ Modules 00-07 completed Java 21+ syntax (records, var , pattern matching) Spring Boot conventions (beans, properties, SLF4J logging) Understanding of the Order Management System domain model 1. What Is Reactive Programming? ¶ Reactive programming is a programming paradigm built around asynchronous data streams . Instead of pulling data from a source and blocking until it arrives, you declare a pipeline of operations that react to data as it flows through. Imperative vs Reactive — An Analogy ¶ Imperative (Blocking) Reactive (Non-Blocking) A waiter who takes one order, waits in the kitchen, delivers it, then takes the next order. The whole restaurant stalls while the kitchen cooks. A ticket system where orders are placed on a conveyor belt. Multiple workers pick up tickets as they become ready, processing them in parallel without waiting. Thread.sleep(1000) — the thread is blocked, doing nothing Flux.interval(Duration.ofSeconds(1)) — the thread is free to do other work between emissions You ask for data and wait You subscribe to data and get notified A Concrete Example ¶ Imperative (blocking): // Blocks the calling thread for 3 seconds total List&lt;Order&gt; orders = orderRepository.findAll(); // blocks on DB I/O List&lt;OrderDto&gt; dtos = orders.stream() .map(this::toDto) // blocks on CPU .toList(); return dtos; // caller was waiting the entire time Reactive (non-blocking): // Returns immediately; the caller subscribes and gets notified Flux&lt;OrderDto&gt; dtos = orderRepository.findAll() // returns Flux, no blocking .map(this::toDto) // transforms each item as it arrives .doOnNext(dto -&gt; log.info(\"Processed {}\", dto.id())); // Nothing has executed yet! The caller must .subscribe() return dtos; The key insight: nothing happens until someone subscribes . The reactive pipeline is a declaration of what should happen, not an immediate execution. 2. Why Reactive? The C10K Problem ¶ The C10K problem refers to the challenge of handling 10,000+ concurrent connections on a single server. In the traditional thread-per-request model: Each connection needs a dedicated OS thread Each thread consumes ~1MB of stack memory 10,000 threads = ~10GB of memory just for thread stacks Context switching between 10,000 threads wastes CPU cycles Most threads are blocked on I/O (waiting for database, HTTP, etc.) — doing nothing How Reactive Solves This ¶ Reactive frameworks use a small pool of threads (typically 2x CPU cores) and non-blocking I/O . When a thread initiates an I/O operation, it doesn't wait — it moves on to the next task. When the I/O completes, a callback fires on one of the available threads. Thread-Per-Request (Blocking) Reactive (Non-Blocking) 10,000 connections = 10,000 threads 10,000 connections = ~8 threads ~10GB memory for thread stacks ~8MB memory for thread stacks Threads blocked on I/O waste CPU Threads always doing useful work Scaling requires more hardware Scaling requires better I/O utilization When Java 21 Virtual Threads Help ¶ Java 21's virtual threads (Project Loom) offer an alternative: lightweight threads that can be blocked without wasting OS resources. Virtual threads make blocking code scale — but they don't give you the pipeline composition, backpressure, and stream operators that Reactor provides. Virtual threads and Reactor are complementary, not exclusive. 3. Project Reactor — The Foundation of Spring WebFlux ¶ Project Reactor is a Reactive Streams implementation used by Spring WebFlux. It provides two primary types: Mono&lt;T&gt; — emits zero or one item, then completes (or errors) Flux&lt;T&gt; — emits zero to N items, then completes (or errors) Both implement the Publisher interface from the Reactive Streams spec. Mono vs Flux — When to Use Each ¶ Type Analogy Use Case Mono A single ticket booth — you get one ticket or none Find by ID, save one record, HTTP request returning one response Flux A train line — trains keep arriving, possibly forever List all records, Kafka topic stream, WebSocket messages 4. Creating Mono and Flux ¶ Static Factory Methods ¶ Factory Description Example Mono.just(T) Emits one item and completes Mono.just(\"Hello\") Mono.empty() Emits nothing, completes immediately Mono.empty() Mono.error(Throwable) Emits nothing, signals an error Mono.error(new RuntimeException(\"fail\")) Mono.fromCallable(Callable) Wraps a blocking call, emits its result Mono.fromCallable(() -&gt; repo.findById(id)) Mono.fromSupplier(Supplier) Wraps a lazy value computation Mono.fromSupplier(() -&gt; generateId()) Flux.just(T...) Emits each argument and completes Flux.just(1, 2, 3) Flux.fromIterable(Iterable) Emits each element of a collection Flux.fromIterable(orders) Flux.range(int, int) Emits a range of integers Flux.range(1, 10) Flux.empty() Emits nothing, completes Flux.empty() Flux.error(Throwable) Emits nothing, signals an error Flux.error(new IllegalStateException()) Flux.interval(Duration) Emits 0L, 1L, 2L... at fixed intervals Flux.interval(Duration.ofSeconds(1)) Code Example — Creating Publishers ¶ package com.example.ordermgmt.reactive; import reactor.core.publisher.Flux; import reactor.core.publisher.Mono; import java.math.BigDecimal; import java.time.Duration; import java.util.List; public class PublisherDemo { // Mono: zero or one item public Mono&lt;String&gt; singleGreeting() { return Mono.just(\"Hello, Reactive World!\"); } // Mono: from a blocking call (wraps synchronous code) public Mono&lt;Integer&gt; computeAsync() { return Mono.fromCallable(() -&gt; { Thread.sleep(100); // simulate work return 42; }); } // Flux: emit multiple items public Flux&lt;Integer&gt; numbers() { return Flux.range(1, 10); } // Flux: from a collection public Flux&lt;String&gt; fromList() { return Flux.fromIterable(List.of(\"apple\", \"banana\", \"cherry\")); } // Flux: emit at intervals public Flux&lt;Long&gt; ticker() { return Flux.interval(Duration.ofSeconds(1)) .take(5); // stop after 5 emissions } // Mono: empty (no result) public Mono&lt;Void&gt; logEvent(String event) { return Mono.fromRunnable(() -&gt; System.out.println(\"Event: \" + event)) .then(); // converts to Mono&lt;Void&gt; } } 5. Core Operators ¶ Operators transform, filter, or combine reactive streams. Each operator returns a new Mono or Flux — they never mutate the source. Reference Table ¶ Operator Purpose Input Output map Transforms each item Flux&lt;Integer&gt; Flux&lt;String&gt; flatMap Transforms each item into a new Publisher, flattens Flux&lt;Order&gt; Flux&lt;OrderDto&gt; filter Keeps items matching a predicate Flux&lt;Integer&gt; Flux&lt;Integer&gt; collectList Collects all items into a List Flux&lt;Order&gt; Mono&lt;List&lt;Order&gt;&gt; reduce Reduces to a single value Flux&lt;Integer&gt; Mono&lt;Integer&gt; concat Joins streams sequentially Flux&lt;A&gt;, Flux&lt;A&gt; Flux&lt;A&gt; merge Interleaves streams in real-time Flux&lt;A&gt;, Flux&lt;A&gt; Flux&lt;A&gt; zip Combines items pairwise from two streams Flux&lt;A&gt;, Flux&lt;B&gt; Flux&lt;Tuple2&lt;A,B&gt;&gt; flatMapMany Mono → Flux (one-to-many) Mono&lt;Order&gt; Flux&lt;OrderItem&gt; switchIfEmpty Fallback w"}, {"slug": "09-tdd", "num": "09", "title": "Test-Driven Development", "content": "Module 09: Test-Driven Development ¶ What You'll Learn ¶ What testing is and why we test our code The different types of tests: unit, integration, and end-to-end The test pyramid and why most tests should be unit tests What Test-Driven Development (TDD) is and the Red-Green-Refactor cycle How to write tests with JUnit 5 How to write readable assertions with AssertJ How to mock dependencies with Mockito How to follow TDD step by step to build an OrderCalculator and OrderService Spring Boot test slices: @WebMvcTest , @DataJpaTest , @SpringBootTest How to test REST controllers with MockMvc How to test repositories with Testcontainers How to measure test coverage with JaCoCo How to test Kafka consumers How to test reactive streams with StepVerifier Prerequisites ¶ Module 00: Java Foundations — you understand Java classes, records, exceptions, generics Module 01: Build Tools &amp; Project Setup — you have a Maven project Module 02: Dependency Injection — you understand beans and constructor injection Module 03: Spring Boot Fundamentals — you understand REST controllers and DTOs Module 04: Repository Pattern — you understand JPA entities and repositories Module 05: Service-Oriented Architecture — you understand the service layer and transactions Module 06: Kafka — you understand Kafka producers and consumers Module 08: Reactor Pattern — you understand Mono and Flux (for testing reactive code) 1. What Is Testing? Why Do We Test? ¶ Testing is the deliberate execution of your code to verify it does what you intend it to do. You write a piece of code, then you write another piece of code that calls the first piece and checks whether the result is correct. Why Test? ¶ Reason Explanation Catch bugs early A bug found during development costs minutes to fix. The same bug found in production costs hours or days. Prevent regressions When you change code, old tests verify you didn't break existing behavior. Living documentation Tests show how your code is supposed to be used. A new developer can read tests to understand the system. Design feedback If code is hard to test, it's usually badly designed — too many dependencies, too much coupling. Confidence With good tests, you can refactor and add features without fear of breaking things. Faster debugging When something breaks, a failing test points you to exactly which code is broken. What Happens Without Tests? ¶ You write code and manually test it (click through the UI or use curl) It works, so you move on Six months later, you add a new feature The new feature breaks something you didn't think about The bug reaches production A customer reports it You spend hours debugging because you don't remember how the old code works Tests prevent steps 3 through 7. 2. Types of Tests ¶ Unit Tests ¶ A unit test tests a single piece of code (a single method or class) in isolation. All external dependencies are replaced with mocks (fake versions that return predetermined values). Analogy: Testing a single car part on a workbench — you test whether the alternator produces electricity, without needing the entire car. Speed: milliseconds. You can run hundreds per second. Example: Testing that OrderService.createOrder() throws InsufficientStockException when stock is too low, with a mocked repository that returns a product with low stock. Integration Tests ¶ An integration test tests how multiple components work together. Dependencies are real, not mocked. Analogy: Installing the alternator in the car and testing whether the battery charges when the engine runs. Speed: seconds. They start a Spring context and talk to a real (or test) database. Example: Testing that OrderService.createOrder() saves the order to a real PostgreSQL database running in a Docker container, and the order can be retrieved afterward. End-to-End (E2E) Tests ¶ An E2E test tests the entire system from the outside, simulating a real user. Analogy: Driving the car on the road to see if it starts, accelerates, and brakes. Speed: minutes. They start the entire application and make real HTTP requests. Example: Sending a POST /api/orders request to a running server and verifying the response, the database state, and that a Kafka event was published. 3. The Test Pyramid ¶ The test pyramid is a guideline for how many of each test type you should have: ╱╲ ╱ ╲ E2E Tests (few) ╱────╲ ╱ ╲ Integration Tests (some) ╱────────╲ ╱ ╲ Unit Tests (many) ╱────────────╲ Level Quantity Why Unit tests 80% of tests Fast, isolate bugs, easy to write Integration tests 15% of tests Verify components work together E2E tests 5% of tests Slow, fragile, but verify the whole system Why more unit tests? They're fast (milliseconds), reliable (no external dependencies to fail), and precise (when one fails, you know exactly which code is broken). Integration and E2E tests are slower and when they fail, the cause could be anywhere. 4. What Is TDD? ¶ Test-Driven Development (TDD) is a development practice where you write the test before you write the production code. This sounds backwards, but it's incredibly powerful. The Red-Green-Refactor Cycle ¶ TDD follows a three-step cycle: ┌──────────┐ │ RED │ Write a test that fails (because the code doesn't exist yet) └────┬─────┘ │ ┌────▼─────┐ │ GREEN │ Write the minimum code to make the test pass └────┬─────┘ │ ┌────▼─────┐ │ REFACTOR │ Improve the code without changing behavior (tests stay green) └────┬─────┘ │ └────&gt;──── (repeat) Red: You write a test that describes what you want the code to do. You run it. It fails because the code doesn't exist yet. This is called \"red\" because the test runner shows red (failure). Green: You write the minimum code to make the test pass. This isn't about perfect code — it's about making the test pass as quickly as possible. You run the test. It passes (green). Refactor: Now that the test passes, you improve the code — rename variables, extract methods, remove duplication — without changing behavior. You run the test after each change. It stays green, proving you didn't break anything. Why TDD? ¶ You never forget to write tests — they come first, not last The code is testable by design — if you write the test first, the code is naturally testable You only write code that's needed — the test defines exactly what's needed; no speculative features Instant feedback — you know within seconds whether your code works Living documentation — tests describe what the code does, in executable form 5. JUnit 5 ¶ JUnit 5 is the most popular testing framework for Java. It provides annotations and classes for writing and running tests. Core Annotations ¶ Annotation Purpose @Test Marks a method as a test @DisplayName Gives a test a human-readable name @BeforeEach Runs before each test — use for setup @AfterEach Runs after each test — use for cleanup @BeforeAll Runs once before all tests — must be static @AfterAll Runs once after all tests — must be static @ParameterizedTest Runs the same test with different inputs @Nested Groups related tests in an inner class @Disabled Skips a test A Simple Test Class ¶ package com.example.ordermgmt.service; import org.junit.jupiter.api.DisplayName; import org.junit.jupiter.api.Test; import org.junit.jupiter.api.BeforeEach; import static org.assertj.core.api.Assertions.assertThat; class OrderCalculatorTest { private OrderCalculator calculator; @BeforeEach void setUp() { // This runs before every test calculator = new OrderCalculator(); } @Test @DisplayName(\"Should calculate total for a single item\") void shouldCalculateTotalForSingleItem() { // Arrange BigDecimal unitPrice = new BigDecimal(\"49.99\"); int quantity = 3; // Act BigDecimal total = calculator.calculateTotal(unitPrice, quantity); // Assert assertThat(total).isEqualByComparingTo(\"149.97\"); } @Test @DisplayName(\"Should return zero when quantity is zero\") void shouldReturnZeroWhenQuantityIsZero() { BigDecimal unitPrice = new BigDecimal(\"99.99\"); int quantity = 0; BigDecimal total = calculator.calculateTotal(unitPrice, quantity); assertThat(t"}, {"slug": "10-capstone-project", "num": "10", "title": "Capstone Project", "content": "Module 10: Capstone Project ¶ What You'll Learn ¶ How to assemble a complete Order Management System using all technologies from the course How REST and GraphQL APIs coexist in one Spring Boot application How to publish Kafka events from domain services and consume them How to stream order-status updates with Spring WebFlux (reactive programming) How to structure a project with layered architecture (Controller, Service, Repository, Domain) How to configure JPA, GraphQL, Kafka, WebFlux, Docker, and Testcontainers How to test all layers (unit, integration, Kafka, reactive) How to run and test the application with curl and GraphQL queries Prerequisites ¶ All prior modules (00-09) must be completed Java 21+, Maven, Docker Desktop installed Understanding of: Java syntax, Spring Boot DI, REST controllers, JPA entities, service layer, Kafka, GraphQL, Reactor (Mono/Flux), TDD 1. Project Overview ¶ The Order Management System (OMS) is a backend service for an e-commerce platform. It manages: Customers who place orders Products with stock levels Orders containing multiple order items Kafka events when orders are created or status changes REST + GraphQL APIs for clients Reactive streaming for real-time order status updates Architecture Diagram ¶ ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ │ REST Client │ │ GraphQL │ │ Reactive │ │ (curl, browser)│ │ Client │ │ Subscriber │ └───────┬────────┘ └───────┬────────┘ └───────┬────────┘ │ │ │ │ HTTP │ /graphql │ WebSocket │ │ │ /graphql ┌───────▼─────────────────────▼─────────────────────▼────────┐ │ Spring Boot Application │ │ │ │ ┌───────────────┐ ┌──────────────┐ ┌────────────────┐ │ │ │ OrderController│ │OrderResolver │ │OrderFlux │ │ │ │ (REST) │ │(GraphQL) │ │(WebFlux) │ │ │ └───────┬───────┘ └──────┬───────┘ └───────┬────────┘ │ │ │ │ │ │ │ ┌───────▼──────────────────▼───────────────────▼────────┐│ │ │ OrderService ││ │ │ (business logic, transactions, event publishing) ││ │ └───────┬──────────────┬─────────────────────────────────┘│ │ │ │ │ │ ┌───────▼───────┐ ┌───▼──────────────────┐ │ │ │ Repositories │ │ OrderEventProducer │ │ │ │ (JPA) │ │ (Kafka) │ │ │ └───────┬───────┘ └───────┬──────────────┘ │ │ │ │ │ └──────────┼───────────────────┼───────────────────────────────┘ │ │ ┌───────▼───────┐ ┌───────▼───────┐ │ PostgreSQL │ │ Kafka │ │ (database) │ │ (broker) │ └───────────────┘ └───────┬───────┘ │ ┌───────▼───────┐ │OrderEvent │ │Consumer │ │(notifications) │ └───────────────┘ 2. Complete Project Structure ¶ ordermgmt/ ├── src/ │ ├── main/ │ │ ├── java/com/example/ordermgmt/ │ │ │ ├── OrderManagementApplication.java │ │ │ ├── config/ │ │ │ │ └── KafkaConfig.java │ │ │ ├── controller/ │ │ │ │ ├── OrderController.java │ │ │ │ └── GlobalExceptionHandler.java │ │ │ ├── domain/ │ │ │ │ ├── CustomerEntity.java │ │ │ │ ├── ProductEntity.java │ │ │ │ ├── OrderEntity.java │ │ │ │ ├── OrderItemEntity.java │ │ │ │ └── OrderStatus.java │ │ │ ├── dto/ │ │ │ │ ├── CreateOrderRequest.java │ │ │ │ └── OrderResponse.java │ │ │ ├── graphql/ │ │ │ │ ├── OrderQueryResolver.java │ │ │ │ └── OrderMutationResolver.java │ │ │ ├── kafka/ │ │ │ │ ├── OrderEventProducer.java │ │ │ │ ├── OrderEventConsumer.java │ │ │ │ └── event/ │ │ │ │ ├── OrderCreatedEvent.java │ │ │ │ └── OrderStatusChangedEvent.java │ │ │ ├── repository/ │ │ │ │ ├── CustomerRepository.java │ │ │ │ ├── ProductRepository.java │ │ │ │ └── OrderRepository.java │ │ │ └── service/ │ │ │ ├── OrderService.java │ │ │ └── exception/ │ │ │ └── OrderNotFoundException.java │ │ └── resources/ │ │ ├── application.yml │ │ └── graphql/ │ │ └── schema.graphqls │ └── test/java/com/example/ordermgmt/ │ ├── service/OrderServiceTest.java │ ├── controller/OrderControllerTest.java │ └── repository/OrderRepositoryTest.java ├── pom.xml └── docker-compose.yml 3. pom.xml ¶ &lt;?xml version=\"1.0\" encoding=\"UTF-8\"?&gt; &lt;project xmlns=\"http://maven.apache.org/POM/4.0.0\" xmlns:xsi=\"http://www.w3.org/2001/XMLSchema-instance\" xsi:schemaLocation=\"http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd\"&gt; &lt;modelVersion&gt;4.0.0&lt;/modelVersion&gt; &lt;parent&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-parent&lt;/artifactId&gt; &lt;version&gt;3.3.4&lt;/version&gt; &lt;relativePath/&gt; &lt;/parent&gt; &lt;groupId&gt;com.example&lt;/groupId&gt; &lt;artifactId&gt;ordermgmt&lt;/artifactId&gt; &lt;version&gt;1.0.0&lt;/version&gt; &lt;packaging&gt;jar&lt;/packaging&gt; &lt;name&gt;Order Management System&lt;/name&gt; &lt;properties&gt; &lt;java.version&gt;21&lt;/java.version&gt; &lt;/properties&gt; &lt;dependencies&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-data-jpa&lt;/artifactId&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.postgresql&lt;/groupId&gt; &lt;artifactId&gt;postgresql&lt;/artifactId&gt; &lt;scope&gt;runtime&lt;/scope&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-graphql&lt;/artifactId&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.kafka&lt;/groupId&gt; &lt;artifactId&gt;spring-kafka&lt;/artifactId&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-webflux&lt;/artifactId&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-devtools&lt;/artifactId&gt; &lt;scope&gt;runtime&lt;/scope&gt; &lt;optional&gt;true&lt;/optional&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt; &lt;scope&gt;test&lt;/scope&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.springframework.kafka&lt;/groupId&gt; &lt;artifactId&gt;spring-kafka-test&lt;/artifactId&gt; &lt;scope&gt;test&lt;/scope&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;io.projectreactor&lt;/groupId&gt; &lt;artifactId&gt;reactor-test&lt;/artifactId&gt; &lt;scope&gt;test&lt;/scope&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.testcontainers&lt;/groupId&gt; &lt;artifactId&gt;postgresql&lt;/artifactId&gt; &lt;scope&gt;test&lt;/scope&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.testcontainers&lt;/groupId&gt; &lt;artifactId&gt;junit-jupiter&lt;/artifactId&gt; &lt;scope&gt;test&lt;/scope&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.testcontainers&lt;/groupId&gt; &lt;artifactId&gt;kafka&lt;/artifactId&gt; &lt;scope&gt;test&lt;/scope&gt; &lt;/dependency&gt; &lt;/dependencies&gt; &lt;build&gt; &lt;plugins&gt; &lt;plugin&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-maven-plugin&lt;/artifactId&gt; &lt;/plugin&gt; &lt;/plugins&gt; &lt;/build&gt; &lt;/project&gt; 4. application.yml ¶ server: port: 8080 spring: application: name: Order Management System datasource: url: jdbc:postgresql://localhost:5432/ordermgmt username: postgres password: postgres driver-class-name: org.postgresql.Driver jpa: hibernate: ddl-auto: update show-sql: true properties: hibernate: format_sql: true graphql: graphiql: enabled: true path: /graphiql kafka: bootstrap-servers: localhost:9092 producer: key-serializer: org.apache.kafka.common.serialization.StringSerializer value-serializer: org.springframework.kafka.support.serializer.JsonSerializer consumer: group-id: ordermgmt-group key-deserializer: org.apache.kafka.common.serialization.StringDeserializer value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer auto-offset-reset: earliest properties: spring.json.trusted.packages: \"com.example.ordermgmt.kafka.event\" logging: le"}, {"slug": "11-migrating-java-to-kotlin", "num": "11", "title": "Migrating Java to Kotlin", "content": "Module 11: Migrating Java to Kotlin ¶ What You'll Learn ¶ Why Kotlin is a compelling alternative to Java for Spring Boot applications How Kotlin's syntax compares to Java — line-by-line equivalents How to add Kotlin to an existing Spring Boot Maven project How to convert Java entities, DTOs, services, and controllers to Kotlin How Kotlin's null safety system prevents NullPointerException at compile time How to use Kotlin coroutines as an alternative to Reactor's Mono/Flux How Kotlin data classes , extension functions , and DSLs simplify code How to use Kotlin-specific Spring Boot features (routing DSL, coroutine repositories) A step-by-step migration strategy with automated tools (J2K converter) How to test Kotlin code with JUnit 5, MockK, and kotest-assertions Prerequisites ¶ All prior modules (00-10) must be completed Java 21+, Maven, and an IDE (IntelliJ IDEA Community or Ultimate) Understanding of: Java OOP, Spring Boot DI/REST/JPA, records, Reactor Mono/Flux 1. Why Kotlin? ¶ Kotlin is a modern, statically-typed programming language that runs on the JVM. It was created by JetBrains (the company behind IntelliJ IDEA) and is fully interoperable with Java — you can call Java code from Kotlin and vice versa without any wrappers or adapters. Google made Kotlin the preferred language for Android development in 2019, and Spring Boot added first-class Kotlin support starting with Spring Framework 5.0 (2017). Today, many production Spring Boot applications run on Kotlin. Java vs Kotlin: Quick Comparison ¶ Feature Java 21 Kotlin Null safety Optional API (runtime) Built-in type system (compile-time) Data classes Records (Java 16+) data class (since Kotlin 1.0) String interpolation String.format() or concatenation \"Hello $name\" Smart casting instanceof + cast is check, automatic cast Extension functions Not available Built-in Coroutines Virtual threads (Java 21) suspend functions, structured concurrency Pattern matching Switch expressions (Java 21) when expression Properties Getters/setters boilerplate val / var — getters/setters auto-generated Default arguments Not available Built-in Named arguments Not available Built-in Sealed classes Sealed interfaces (Java 17+) sealed class (since Kotlin 1.0) Collection literals List.of() , Map.of() listOf() , mapOf() Should You Migrate? ¶ You should NOT migrate every Java project to Kotlin. Consider Kotlin when: Starting a new microservice in an existing Java/Spring ecosystem Adding new features to a project where the team wants to gradually adopt Kotlin You need concise code with fewer bugs (null safety, immutability by default) Your team values expressiveness (DSLs, extension functions, coroutines) You should NOT migrate when: - Your team has no Kotlin experience and ramp-up time is not acceptable - You rely heavily on annotation processing tools that don't support Kotlin well - Your codebase is stable and low-churn — migration effort outweighs benefits 2. Setting Up Kotlin in a Spring Boot Project ¶ Option A: Start a New Project with Spring Initializr ¶ Go to start.spring.io , select: Language : Kotlin Project : Maven Spring Boot : 3.3.x Packaging : Jar Java version : 21 Add dependencies: Spring Web, Spring Data JPA, PostgreSQL Driver, Spring for GraphQL, Spring for Apache Kafka, Spring Boot DevTools. Option B: Add Kotlin to an Existing Java Project ¶ Add the Kotlin Maven plugin and standard library to your pom.xml : &lt;properties&gt; &lt;java.version&gt;21&lt;/java.version&gt; &lt;kotlin.version&gt;2.0.21&lt;/kotlin.version&gt; &lt;/properties&gt; &lt;dependencies&gt; &lt;dependency&gt; &lt;groupId&gt;org.jetbrains.kotlin&lt;/groupId&gt; &lt;artifactId&gt;kotlin-stdlib&lt;/artifactId&gt; &lt;version&gt;${kotlin.version}&lt;/version&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.jetbrains.kotlin&lt;/groupId&gt; &lt;artifactId&gt;kotlin-reflect&lt;/artifactId&gt; &lt;version&gt;${kotlin.version}&lt;/version&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;com.fasterxml.jackson.module&lt;/groupId&gt; &lt;artifactId&gt;jackson-module-kotlin&lt;/artifactId&gt; &lt;/dependency&gt; &lt;/dependencies&gt; &lt;build&gt; &lt;plugins&gt; &lt;plugin&gt; &lt;groupId&gt;org.jetbrains.kotlin&lt;/groupId&gt; &lt;artifactId&gt;kotlin-maven-plugin&lt;/artifactId&gt; &lt;version&gt;${kotlin.version}&lt;/version&gt; &lt;configuration&gt; &lt;compilerPlugins&gt; &lt;plugin&gt;spring&lt;/plugin&gt; &lt;plugin&gt;jpa&lt;/plugin&gt; &lt;plugin&gt;all-open&lt;/plugin&gt; &lt;/compilerPlugins&gt; &lt;jvmTarget&gt;21&lt;/jvmTarget&gt; &lt;/configuration&gt; &lt;executions&gt; &lt;execution&gt; &lt;id&gt;compile&lt;/id&gt; &lt;goals&gt;&lt;goal&gt;compile&lt;/goal&gt;&lt;/goals&gt; &lt;configuration&gt; &lt;sourceDirs&gt; &lt;sourceDir&gt;src/main/kotlin&lt;/sourceDir&gt; &lt;sourceDir&gt;src/main/java&lt;/sourceDir&gt; &lt;/sourceDirs&gt; &lt;/configuration&gt; &lt;/execution&gt; &lt;execution&gt; &lt;id&gt;test-compile&lt;/id&gt; &lt;goals&gt;&lt;goal&gt;test-compile&lt;/goal&gt;&lt;/goals&gt; &lt;configuration&gt; &lt;sourceDirs&gt; &lt;sourceDir&gt;src/test/kotlin&lt;/sourceDir&gt; &lt;sourceDir&gt;src/test/java&lt;/sourceDir&gt; &lt;/sourceDirs&gt; &lt;/configuration&gt; &lt;/execution&gt; &lt;/executions&gt; &lt;dependencies&gt; &lt;dependency&gt; &lt;groupId&gt;org.jetbrains.kotlin&lt;/groupId&gt; &lt;artifactId&gt;kotlin-maven-allopen&lt;/artifactId&gt; &lt;version&gt;${kotlin.version}&lt;/version&gt; &lt;/dependency&gt; &lt;dependency&gt; &lt;groupId&gt;org.jetbrains.kotlin&lt;/groupId&gt; &lt;artifactId&gt;kotlin-maven-noarg&lt;/artifactId&gt; &lt;version&gt;${kotlin.version}&lt;/version&gt; &lt;/dependency&gt; &lt;/dependencies&gt; &lt;/plugin&gt; &lt;plugin&gt; &lt;groupId&gt;org.apache.maven.plugins&lt;/groupId&gt; &lt;artifactId&gt;maven-compiler-plugin&lt;/artifactId&gt; &lt;executions&gt; &lt;execution&gt; &lt;id&gt;default-compile&lt;/id&gt; &lt;phase&gt;none&lt;/phase&gt; &lt;/execution&gt; &lt;execution&gt; &lt;id&gt;default-testCompile&lt;/id&gt; &lt;phase&gt;none&lt;/phase&gt; &lt;/execution&gt; &lt;execution&gt; &lt;id&gt;java-compile&lt;/id&gt; &lt;phase&gt;compile&lt;/phase&gt; &lt;goals&gt;&lt;goal&gt;compile&lt;/goal&gt;&lt;/goals&gt; &lt;/execution&gt; &lt;execution&gt; &lt;id&gt;java-test-compile&lt;/id&gt; &lt;phase&gt;test-compile&lt;/phase&gt; &lt;goals&gt;&lt;goal&gt;testCompile&lt;/goal&gt;&lt;/goals&gt; &lt;/execution&gt; &lt;/executions&gt; &lt;/plugin&gt; &lt;/plugins&gt; &lt;/build&gt; The Three Kotlin Compiler Plugins ¶ These plugins are essential for Spring Boot + Kotlin: spring (allopen) — Opens classes and methods that Spring needs to proxy (e.g., @Service , @Configuration , @RestController ). Kotlin classes are final by default, but Spring creates subclass proxies via CGLIB. jpa (no-arg) — Generates a synthetic no-argument constructor for classes annotated with @Entity . JPA requires this, but Kotlin classes don't have implicit no-arg constructors. all-open — A general plugin that opens classes annoted with any annotation you specify. Used alongside spring for custom annotations. Without these plugins, Spring Boot will fail to start with Kotlin. 3. Kotlin Syntax Crash Course for Java Developers ¶ 3.1 Hello World ¶ Java: public class HelloWorld { public static void main(String[] args) { System.out.println(\"Hello, World!\"); } } Kotlin: fun main() { println(\"Hello, World!\") } Key differences: - No class wrapper needed — top-level fun main() is valid - System.out.println → println - No String[] args required (optional) 3.2 Variables ¶ Java: String name = \"Alice\"; // mutable final int age = 30; // immutable var city = \"NYC\"; // type inferred (Java 10+) Kotlin: var name: String = \"Alice\" // mutable val age: Int = 30 // immutable (preferred) val city = \"NYC\" // type inferred Kotlin uses val (read-only, like Java's final ) and var (mutable). Prefer val — immutability by default is a Kotlin best practice. 3.3 Null Safety ¶ This is Kotlin's headline feature. In Java, any object referen"}];

// Simple full-text search engine
function search(query) {
  if (!query || query.trim().length < 2) return [];

  const terms = query.toLowerCase().trim().split(/\s+/);
  const results = [];

  for (const page of SEARCH_INDEX) {
    const titleLower = page.title.toLowerCase();
    const contentLower = page.content.toLowerCase();

    let score = 0;
    let firstMatch = -1;
    let matchedTerms = 0;

    for (const term of terms) {
      const titleIdx = titleLower.indexOf(term);
      const contentIdx = contentLower.indexOf(term);

      if (titleIdx >= 0) {
        score += 10;
        matchedTerms++;
      }
      if (contentIdx >= 0) {
        score += 1;
        matchedTerms++;
        if (firstMatch < 0 || contentIdx < firstMatch) {
          firstMatch = contentIdx;
        }
      }
    }

    if (matchedTerms > 0) {
      // Boost for matching all terms
      if (matchedTerms >= terms.length) score *= 2;

      // Build snippet
      let snippet = '';
      if (firstMatch >= 0) {
        const start = Math.max(0, firstMatch - 50);
        const end = Math.min(page.content.length, firstMatch + 80);
        snippet = (start > 0 ? '...' : '') + page.content.substring(start, end) + (end < page.content.length ? '...' : '');
      }

      results.push({ ...page, score, snippet });
    }
  }

  results.sort((a, b) => b.score - a.score);
  return results.slice(0, 10);
}

// Wire up the UI
document.addEventListener('DOMContentLoaded', () => {
  const input = document.getElementById('searchInput');
  const resultsDiv = document.getElementById('searchResults');

  if (!input || !resultsDiv) return;

  let debounceTimer;

  input.addEventListener('input', () => {
    clearTimeout(debounceTimer);
    debounceTimer = setTimeout(() => {
      const query = input.value.trim();
      if (query.length < 2) {
        resultsDiv.classList.remove('visible');
        resultsDiv.innerHTML = '';
        return;
      }

      const results = search(query);
      if (results.length === 0) {
        resultsDiv.innerHTML = '<div class="search-no-results">No results found</div>';
      } else {
        resultsDiv.innerHTML = results.map(r => `
          <a href="${r.slug}.html" class="search-result-item">
            <div><span class="sr-num">${r.num}</span><span class="sr-title">${r.title}</span></div>
            <div class="sr-snippet">${r.snippet}</div>
          </a>
        `).join('');
      }
      resultsDiv.classList.add('visible');
    }, 200);
  });

  // Close on click outside
  document.addEventListener('click', (e) => {
    if (!e.target.closest('.search-container')) {
      resultsDiv.classList.remove('visible');
    }
  });

  // Keyboard shortcut: / to focus search
  document.addEventListener('keydown', (e) => {
    if (e.key === '/' && document.activeElement.tagName !== 'INPUT' && document.activeElement.tagName !== 'TEXTAREA') {
      e.preventDefault();
      input.focus();
    }
    if (e.key === 'Escape') {
      resultsDiv.classList.remove('visible');
      input.blur();
    }
  });

  // Sidebar toggle (mobile)
  const toggle = document.getElementById('sidebarToggle');
  const sidebar = document.getElementById('sidebar');
  const overlay = document.getElementById('sidebarOverlay');

  if (toggle && sidebar) {
    toggle.addEventListener('click', () => {
      sidebar.classList.toggle('open');
    });
  }
  if (overlay) {
    overlay.addEventListener('click', () => {
      sidebar.classList.remove('open');
    });
  }
});
